import React, { useContext } from 'react';
import { DataContext } from '../../App';

const SignIn = () => {
    return (
        <div>
            <h1 className='text-2xl text-center'>This is Sign In form page for Post Shop project</h1>
        </div>
    );
};

export default SignIn;